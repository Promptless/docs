---
interface Reason {
  title: string;
  description: string;
}

interface Props {
  reasons?: Reason[];
}

const defaultReasons: Reason[] = [
  {
    title: 'Will my docs turn into AI slop?',
    description:
      "We have a dedicated agent that learns your style from existing docs drafts new ones in this voice. You can take a look at Promptless's docs, it's entirely generated by AI agents, but doesn\'t sound AI-sloppy.",
  },
  {
    title: 'If I connect Promptless to listen for PRs or Slack messages, will it be too noisy?',
    description:
      'Our customers seem to think that Promptless generates just the right amount of suggestions. Sometimes if the context is not complete, the agent might decide to err on the generating docs side, but it\'s very easy to reject changes in Promptless.',
  },
  {
    title: 'I don\'t want another tool, there are already too many SaaS tools that my team have to deal with everyday.',
    description:
      "Many of Promptless\'s customer never had to come back to the Promptless UI after setup - they can do everything from drafting, editing, to publishing right in their github.",
  },
  {
    title: 'Will there be hallucinations?',
    description:
      'The Promptless agent actually writes citations as it generates doc updates. You\'ll be able to see, down to the exact line in the code, where the doc update comes from.',
  },
];

const { reasons = defaultReasons } = Astro.props;
---

<section id="why-promptless" class="pl-site-why" aria-label="Why Promptless?">
  <h2>Why Promptless?</h2>
  <p class="pl-site-why-intro">Real questions that our customers asked us before they became Promptless customers</p>
  <div class="pl-site-why-grid">
    {
      reasons.map((reason) => (
        <article class="pl-site-why-card">
          <h3>{reason.title}</h3>
          <p>{reason.description}</p>
        </article>
      ))
    }
  </div>
</section>
